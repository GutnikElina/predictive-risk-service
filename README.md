# Predictive Risk Service

A repository for delivery risks calculation.

## Getting started

Make sure you have setup your local Git Hooks:

```shell 
git config core.hooksPath .githooks
```

To make all hook files executable run this:

```shell
chmod +x .githooks/*
```